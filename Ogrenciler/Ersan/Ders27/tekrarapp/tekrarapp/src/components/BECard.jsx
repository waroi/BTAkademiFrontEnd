import Card from "react-bootstrap/Card";
import styled from "styled-components";

const BECard = styled(Card)`
  transition: 0.5s;
  :hover {
    transform: scale(1.1);
  }
`;

export default BECard;
